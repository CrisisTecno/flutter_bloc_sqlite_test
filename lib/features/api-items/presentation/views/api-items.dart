

import 'package:flutter/material.dart';

class ApiItemsView extends StatelessWidget {
  const ApiItemsView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Api-Items Views'),
    );
  }
}