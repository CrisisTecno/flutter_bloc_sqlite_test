

import 'package:flutter/material.dart';

class PrefsView extends StatelessWidget {
  const PrefsView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Preferencias Views'),
    );
  }
}