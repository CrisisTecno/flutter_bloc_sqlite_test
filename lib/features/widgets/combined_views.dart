


import 'package:flutter/material.dart';

class CombinedViews extends StatelessWidget {
  const CombinedViews({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Combined Views"),
    );
  }
}